{
  "extends": ["github>macchiitaka/renovate-config", ":assignee(macchiitaka)"],
  "rangeStrategy": "auto",
  "packageRules": [
    {
      "description": "enable package.json > engines update",
      "matchDepTypes": ["engines"],
      "enabled": true
    }
  ]
}
